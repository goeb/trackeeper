
REST API
----------
http://localhost:8000/

GET
    /                         : list of projects

    /projectName/             : home page of project
    /projectName/issue/[list] : list of issues
        optional query string :
        search=text
        sort=id+title-owner : id ascending, then title ascending, then owner descending
        colspec=Stars+ID+Type+Status+Priority+Owner+Summary
        filter=label%3Av1.0+status%3Aopen+cc%3Ayyyyyyyyyyyuuuu
        
    /projectName/issue/N      : details of issue N
    /projectName/issue/search : search form
    /projectName/issue/new    : form for adding a new issue

    ? question here : are users global to all projects or local per project?
    /projectName/user/John Smith : details of user John Smith
    /projectName/login        : login form

    /projectName/structure    : form for modifying database structure
        add/delete field
        add/delete values in predefined lists

POST
    /projectName/issue/new    : add new issue
    /projectName/issue/N      : modify issue N
    /projectName/structure    : modify database structure

    ? same question...
    /login
        user=
        password=
        result: list of projects
    /projectName/user/John Smith : modify user John Smith's details



--
erl -pa ~/erlang-sqlite3/ebin
1> sqlite3:open(db, [{file, "tke/db"}]).
{ok,<0.35.0>}
2> sqlite3:open(db, [{file, "tke/db"}]).
{error,{already_started,<0.35.0>}}


---

4> proplists:get_value( columns, L).
["rowid","title","owner","status"]
6> proplists:get_value( rows, L).      
[{1,<<"critical error should be simple error">>,1,1},
 {2,<<"typo in comment">>,1,1},
 {3,<<"Serious slow-down at startup">>,1,1},
 {4,<<"empty plc database">>,1,1}]


---

how to distinguish html rendering, erlang, xml, csv, json ?
how the client should indicate its choice ?
    ?format=cvs ?format=xml etc.


-- user authentication

Login HTML page 

