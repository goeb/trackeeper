-record(webctx, {project, sid, upath, params}).
% sid = session id (used by module tke_user)
% upath = path of the resource + query string of the 'GET' URL
% params = proplist made from the query string

